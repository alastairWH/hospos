# Example .env file for HOSPOS Backend

# MongoDB connection string
MONGODB_URI=mongodb://localhost:27017

# The database name (optional, default is hospos)
MONGODB_DB=hospos

# Server port (optional, default is 8080)
PORT=8080
